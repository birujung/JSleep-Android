<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/search_button"
        android:icon="@drawable/search"
        android:gravity="center"
        android:title="Search Button"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="collapseActionView|always"/>

    <item android:id="@+id/person_button"
        android:icon="@drawable/account"
        android:gravity="center"
        android:title="Profile Button"
        app:showAsAction="collapseActionView|always"/>

    <item android:id="@+id/add_box_button"
        android:icon="@drawable/add_room"
        android:gravity="center"
        android:title="Create Button"
        app:showAsAction="collapseActionView|always"/>
</menu>